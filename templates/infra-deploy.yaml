steps:
- task: qetza.replacetokens.replacetokens-task.replacetokens@3
  displayName: 'Replace tokens in source/infra/*.json'
  inputs:
    rootDirectory: 'source/infra'
    targetFiles: '**/*.json'

- task: AzureResourceGroupDeployment@2
  displayName: 'Deploy infra-log-analytics on $(Infra.Environment)-$(Infra.Project)-infra-$(Infra.Environment)'
  inputs:
    azureSubscription: '$(Azure.ServiceConnectionId)'
    resourceGroupName: '$(Infra.Environment)-$(Infra.Project)-infra-$(Infra.Environment)'
    location: 'North Europe'
    csmFile: 'source/infra/infra-log-analytics.json'
    csmParametersFile: 'source/infra/infra-log-analytics.parameters.json'
    deploymentMode: Incremental   

- task: AzureResourceGroupDeployment@2
  displayName: 'Deploy infra-app-insights on $(Infra.Environment)-$(Infra.Project)-infra-$(Infra.Environment)'
  inputs:
    azureSubscription: '$(Azure.ServiceConnectionId)'
    resourceGroupName: '$(Infra.Environment)-$(Infra.Project)-infra-$(Infra.Environment)'
    location: 'North Europe'
    csmFile: 'source/infra/infra-app-insights.json'
    csmParametersFile: 'source/infra/infra-app-insights.parameters.json'
    deploymentMode: Incremental

- task: AzureResourceGroupDeployment@2
  displayName: 'Deploy infra-service-bus on $(Infra.Environment)-$(Infra.Project)-infra-$(Infra.Environment)'
  inputs:
    azureSubscription: '$(Azure.ServiceConnectionId)'
    resourceGroupName: '$(Infra.Environment)-$(Infra.Project)-infra-$(Infra.Environment)'
    location: 'North Europe'
    csmFile: 'source/infra/infra-service-bus.json'
    csmParametersFile: 'source/infra/infra-service-bus.parameters.json'
    deploymentMode: Validation     

- task: AzureResourceGroupDeployment@2
  displayName: 'Deploy infra-key-vault on $(Infra.Environment)-$(Infra.Project)-infra-$(Infra.Environment)'
  inputs:
    azureSubscription: '$(Azure.ServiceConnectionId)'
    resourceGroupName: '$(Infra.Environment)-$(Infra.Project)-infra-$(Infra.Environment)'
    location: 'North Europe'
    csmFile: 'source/infra/infra-key-vault.json'
    csmParametersFile: 'source/infra/infra-key-vault.parameters.json'
    deploymentMode: Validation  

- task: AzureResourceGroupDeployment@2
  displayName: 'Deploy infra-apim on $(Infra.Environment)-$(Infra.Project)-infra-$(Infra.Environment)'
  inputs:
    azureSubscription: '$(Azure.ServiceConnectionId)'
    resourceGroupName: '$(Infra.Environment)-$(Infra.Project)-infra-$(Infra.Environment)'
    location: 'North Europe'
    csmFile: 'source/infra/infra-apim.json'
    csmParametersFile: 'source/infra/infra-apim.parameters.json'
    deploymentMode: Validation
